# 백테스팅 시뮬레이터 구현 체크리스트

원본 프로토타입(`app.js`)의 로직을 Next.js 앱으로 포팅하는 작업 목록입니다.
전체 시스템은 ①정규화 → ②peak-trough 낙폭 탐지 → ③CAGR 복리 환산이라는
3개 원자 연산 위에, ④그리드서치+파레토 프론티어, ⑤워크포워드 롤링
백테스트, ⑥DCA/리밸런싱/트리거 매수 시뮬레이션이 조합되는 구조입니다.
뒤 항목일수록 앞 항목을 재사용하므로 번호 순서대로 진행을 권장합니다.

완료된 항목은 `[x]`로 표시되어 있고, 관련 파일 경로를 함께 적어뒀습니다.

---

## 1. 정규화(지수화) — 모든 차트의 기반

기준일 가격을 10,000으로 놓고 상대 변화율로 환산. 가중 포트폴리오는
개별 자산 정규화 값의 비중 가중합(산술 가중합 — 리밸런싱 없이는 시간이
지나면 실제 비중이 흘러감).

- [x] 자산별 가격을 기준일 대비 상대값으로 정규화 — `app/lib/portfolio.ts`의 `buildNormalizedSeries`
- [x] 가중 포트폴리오 = 개별 자산 정규화값의 비중 가중합
- [x] 스크롤/줌 시 현재 보이는 첫 날짜 기준으로 재정규화(rebase) — `app/components/PortfolioChart.tsx`의 `applyRebase`
- [ ] 최종 수익률(`finalReturn`)을 별도 유틸 함수로 분리해 3번(CAGR)·5번(그리드서치)에서 재사용

## 2. MDD(최대낙폭) 세그먼트 탐지

Peak-trough drawdown 알고리즘. 신고점을 계속 갱신하다가, 낙폭이 임계값
이하로 내려가면 "낙폭 구간"으로 인정하고 구간 내 최저점을 기록, 다시
신고점을 찍으면 "회복 완료"로 구간을 종료. 하나의 값 배열에서 여러 개의
독립된 드로우다운 구간을 O(n) 순차 스캔으로 뽑아내는 구조.

- [x] peak-trough 탐지 1차 버전 — `app/components/PortfolioChart.tsx`의 `findPeaksAndTroughs` (그래프 마커용, 임계값 5%)
- [ ] 임계값을 설정 가능하게 (현재 5% 고정 — 원본은 -10%, 필요시 UI에서 조절)
- [x] "구간" 단위(peakDate → troughDate → recoveryDate, 낙폭%, 낙폭기간, 회복기간, 전체소요기간)로 데이터를 반환하는 공용 함수 — `app/lib/mdd.ts`의 `calcMDDSegments` (합성 데이터 5개 케이스로 검증 완료: 정상 회복/얕은 하락 제외/미회복-하락멈춤/미회복-계속하락/연속 두 사이클)
- [x] 자산별 "주요 하락 통계" 테이블 UI (시작일 / 저점일 / 회복일 / 낙폭률 / 낙폭기간 / 회복기간 / 전체소요) — `PortfolioChart`의 성장 그래프 탭 하단
- [ ] `findPeaksAndTroughs`(마커용)를 `calcMDDSegments` 기반으로 통합 — 지금은 로직이 병렬로 중복되어 있음(안전하게 가려고 기존 마커 코드는 안 건드림). 6번(롤링) · 8번(현금버퍼)에서도 아직 미재사용

## 3. CAGR (연평균 복리 수익률)

```
CAGR = (1 + 최종수익률)^(1/투자년수) - 1
```

단순 산술평균이 아니라 기하평균(복리) 기반이라 기간이 다른
포트폴리오/자산 간 수익률을 공정하게 비교 가능.

- [x] CAGR 계산 유틸 함수 작성 — `app/lib/metrics.ts`의 `computeRollingCAGR`/`summarizeRollingCAGR` (고정 보유기간으로 시작일을 롤링하며 실제 경과일수 기준 복리 계산, 알려진 CAGR 합성 데이터로 검증 완료)
- [x] 포트폴리오 전체 및 개별 자산 각각에 대해 계산 (임의의 `SeriesPoint[]`를 받으므로 재사용 가능)
- [x] UI에 표시 — 별도 탭 대신 5번(위험-수익 탭)의 y축으로 통합 (CAGR 단독 탭은 만들었다가 중복이라 제거함)

## 4. 성장 곡선 + MDD 오버레이

기본 정규화 값을 시계열로 그린 성장 곡선 위에, 각 낙폭 구간을 점/음영으로
오버레이. 호버 시 "며칠 만에 몇 % 빠졌는지" 표시.

- [x] 정규화 값 시계열 성장 차트 — `PortfolioChart`
- [x] 신고점/저점 포인트 마커 + 근접 호버 시 상세정보(줌 레벨에 따라 좌우 1~10개 데이터 포인트 스냅)
- [ ] 마커뿐 아니라 MDD 구간 전체를 라인 위에 음영(영역)으로 표시
- [ ] 호버 시 낙폭 기간(며칠 만에 몇 % 빠졌는지) 상세 정보 추가 — 현재는 낙폭률만 표시, 기간 없음
- [ ] 2번의 공용 `calcMDDSegments` 함수로 교체

## 5. 최적 비중 탐색 (그리드서치 + 파레토 프론티어)

완전 탐색(brute-force)으로 비중을 10%p 단위 조합을 모두 생성 후 각각
finalReturn/CAGR/worstMDD 계산, 파레토 최적(다른 조합에 지배당하지 않는
조합)만 필터링. 마코위츠 효율적 프론티어를 수익률-MDD 평면에 적용한 것과
동일한 개념.

- [x] 그리드서치: 자산 개수만큼 재귀하며 10%p 단위로 합이 100%인 모든 비중 조합 생성 — `app/lib/metrics.ts`의 `generateWeightCombinations` (4자산 286개 = C(13,3), 검증 완료)
- [x] 각 조합에 대해 평균 롤링 CAGR / MDD 계산 (1·2·3번 재사용) — `combineWeightedSeries`(재조합) + `computeRollingCAGR`/`computeMaxDrawdown`
- [x] 파레토 최적 필터링 로직 (지배당하는 조합 제거) — `findParetoFrontier`, x=MDD(위험) y=평균 롤링 수익률 기준으로 검증 완료
- [x] 파레토 산점도 차트 (x축 MDD — 우측이 안전, y축 평균 수익률) + 현재 설정한 비중을 별표로 강조 + 파레토 조합 표 — `PortfolioChart`의 "위험-수익" 탭
- [ ] DCA 버전(7번과 결합) 산점도까지 확장

## 6. 롤링 윈도우 워크포워드 백테스트

`WINDOW_SIZE = 투자기간(년) × 252`, `STEP = 21`(약 1개월)씩 창을 밀며
전체 구간을 스캔. 각 창마다 그리드서치+파레토(5번)를 반복 실행하고
평균 내어, 시작 시점 편향(luck of the entry point)을 완화.

- [x] 롤링 윈도우 슬라이딩 로직 (고정 보유기간, ~1개월 STEP) — `app/lib/metrics.ts`의 `computeRollingCAGR`
- [x] 각 창마다 5번(그리드서치+파레토) 재실행 — "위험-수익" 탭에서 조합마다 `computeRollingCAGR`를 돌려 평균/MDD를 계산하므로 사실상 완료
- [x] 여러 창 결과를 비중 조합별로 평균 내어 "일관되게 좋은 비중 조합" 산출 — 파레토 표가 곧 그 결과
- [ ] 연속년수(보유기간)별로 여러 그래프를 한 번에 비교하는 UI(예: 1년/3년/5년 각각의 위험-수익 그래프를 나란히)는 아직 — 지금은 보유기간 하나씩만 입력해서 확인

## 7. DCA(적립식) 시뮬레이션

매월 정액 매수(dollar-cost averaging) + 연 1회 리밸런싱(오른 자산을 팔아
떨어진 자산을 사는 평균회귀 효과).

- [x] 매월 정액 매수 시뮬레이션 (보유수량 += 비중×투입액 / 그날가격) — `app/lib/dca.ts`의 `simulateDCA`
- [x] 연 1회 리밸런싱 로직 (목표비중으로 보유수량 재조정) — 12번째 매수마다 전체 평가액을 목표비중대로 재분배
- [x] DCA 성장 곡선 차트 + 리밸런싱 유무 비교 — `PortfolioChart`의 "DCA" 탭 (누적 투자원금 · 리밸런싱 없음 · 연 1회 리밸런싱 3개 라인 + 최종 수익률 표). 합성 데이터(평탄가/상승가/리밸런싱 효과)와 실제 QQQ/GLD 10년 데이터로 검증 완료

## 8. 현금버퍼(전술적 매수) 전략

매월 투입액 일부를 현금 풀로 보유하고, 낙폭이 설정된 트리거(예: -15%,
-25%)에 도달하면 현금 풀 일부를 그 시점에 투입 — "하락할 때 현금을 더
투입한다"는 buy-the-dip 규칙을 낙폭 임계값으로 정량화한 것 (2번의
실시간/온라인 버전).

- [x] 매월 투입액 일부를 현금 풀로 보유, 나머지 즉시 매수 로직 — `app/lib/cashBuffer.ts`의 `simulateCashBuffer`
- [x] 신고점 갱신 시 트리거 리셋
- [x] 낙폭이 트리거 레벨 도달 시 현금 풀 일부 매수 (남은 트리거 개수만큼 균등 분할 배분, 합성 데이터로 트리거 순차 발동·리셋·재발동 검증 완료)
- [x] 순수 DCA vs 현금버퍼 최종수익률/MDD 비교 표 — `PortfolioChart`의 "현금버퍼" 탭 (CAGR 컬럼은 아직, 수익률/MDD만)
- [ ] 별도의 "낙폭 비교" 전용 차트(현금버퍼 총자산 vs 순수 DCA만 놓고 drawdown만 그리는 서브 차트)는 아직 — 지금은 메인 성장곡선 차트 + 요약표로 대체

---

## 권장 진행 순서

1. ~~3 (CAGR)~~ — 완료 (5번에 통합)
2. ~~5 (그리드서치/파레토)~~ — 완료
3. ~~6 (롤링 백테스트)~~ — 완료 (5번과 사실상 동일 로직)
4. ~~7 (DCA)~~ — 완료, "DCA" 탭
5. ~~8 (현금버퍼)~~ — 완료, "현금버퍼" 탭
6. **2 리팩터링** — MDD를 "포인트 마커"가 아닌 "구간" 단위 공용 함수로 (마커용 `findPeaksAndTroughs`를 `calcMDDSegments` 기반으로 통합)
7. **4 마무리** — 위를 이용해 성장곡선에 낙폭 구간 음영 오버레이 + 호버 시 낙폭기간 표시 추가
